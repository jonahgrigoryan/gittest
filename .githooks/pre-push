#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
SCRIPT="$ROOT_DIR/scripts/check-task-handoff-docs.sh"

if [[ ! -x "$SCRIPT" ]]; then
  echo "Handoff check script is not executable: $SCRIPT"
  exit 1
fi

bash "$SCRIPT"
