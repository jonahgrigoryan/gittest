{
  "name": "@poker-bot/executor",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rimraf dist",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@nut-tree/nut-js": "npm:@nut-tree-fork/nut-js@^4.2.6",
    "@poker-bot/shared": "workspace:*"
  },
  "devDependencies": {
    "fast-check": "^4.5.3"
  }
}
