{
  "name": "@poker-bot/orchestrator",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p .",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rimraf dist",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@grpc/grpc-js": "1.11.1",
    "@poker-bot/shared": "workspace:*",
    "@poker-bot/agents": "workspace:*",
    "@poker-bot/executor": "workspace:*"
  }
}
